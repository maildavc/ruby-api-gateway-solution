# Errors

Errors use standard HTTP status codes with a JSON payload.

```json
{
  "error": "invalid_scope",
  "message": "The requested scope is not enabled for this key.",
  "traceId": "tr_8x991z"
}
```

Use the **Logs** page to look up trace IDs.
